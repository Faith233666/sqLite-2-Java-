services:
  user-api:
    build: .
    container_name: user-api
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DB_PATH: /app/data/demo.db
      WEB_ROOT: /app/web
    volumes:
      - user-data:/app/data
    restart: unless-stopped

volumes:
  user-data:
